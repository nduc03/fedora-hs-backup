# General settings
general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL
  use_x_forwarded_for: true
  mcp_trusted_proxy_ranges:
    - 127.0.0.1/32

# Optional: Router settings for load balancing with Redis
# router_settings:
#   redis_host: os.environ/REDIS_HOST
#   redis_password: os.environ/REDIS_PASSWORD
#   redis_port: os.environ/REDIS_PORT
#   routing_strategy: least-busy  # or: simple-shuffle, latency-based-routing

# Optional: Logging
litellm_settings:
  set_verbose: False      # Switch off Debug Logging
#   success_callback: ["langfuse"]  # Track requests
#   failure_callback: ["langfuse"]


# ========================================================
# CÁC MODEL BỊ VÔ HIỆU HÓA HOẶC LÀM VÍ DỤ NẰM Ở DƯỚI ĐÂY
# ========================================================

#   # OpenAI GPT-4o example
#   - model_name: gpt-4o
#     litellm_params:
#       model: openai/gpt-4o
#       api_key: os.environ/OPENAI_API_KEY
#       # Optional: extra headers
#       # extra_headers: {"AI-Resource-Group": "my-resource"}
#       # organization: org-xxxxx
#       temperature: 0.7

#   # Claude example (Anthropic)
#   - model_name: claude-3-5-sonnet
#     litellm_params:
#       model: anthropic/claude-3-5-sonnet-20241022
#       api_key: os.environ/ANTHROPIC_API_KEY
